# Strim Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit the .env file to version control

# PostgreSQL Configuration (required for docker-compose with postgres)
POSTGRES_DB=strimdb
POSTGRES_USER=strim
POSTGRES_PASSWORD=your-secure-password-here

# OAuth Configuration (optional - enables social login)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# MICROSOFT_CLIENT_ID=your-microsoft-client-id
# MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret
# MICROSOFT_TENANT_ID=common

# CORS Configuration (optional - defaults to allow any origin without credentials)
# ALLOWED_ORIGINS=https://your-frontend.com,https://another-allowed.com
